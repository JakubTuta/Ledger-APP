[pytest]
asyncio_mode = auto
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
markers =
    e2e: end-to-end test against the live local stack
    e2e_slow: end-to-end test that waits on a cron-driven background job
